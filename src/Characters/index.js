
export const Characters = ({ data }) => {

    return (
        <h1>Hello</h1>
        // <>
        //     {data.map((character) => (
        //         <div>{character.name}</div>
        //     )
        //     )}
        // </>
    );

};